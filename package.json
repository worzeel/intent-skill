{
  "name": "intent",
  "version": "0.1.0",
  "description": "AI code provenance tracking for Claude Code — captures why code was written, anchored to git blob hashes.",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "build": "bun run scripts/build.mjs",
    "build:all": "bun run scripts/build.mjs --all",
    "bundle": "bun run scripts/bundle.mjs",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "bun test --timeout 20000",
    "test:watch": "bun test --watch --timeout 20000"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "@types/node": "^22.10.5",
    "typescript": "^5.7.3"
  }
}
